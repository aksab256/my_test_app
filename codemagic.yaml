workflows:
flutter-android-build:
name: Flutter Android Release Build
max_build_duration: 60
environment:
flutter: stable
java: 17
# === التحديث الضروري: إضافة NDK المطلوب لتجنب خطأ InstallFailedException ===
android_sdk:
- ndk;28.2.13676358
# =========================================================================
scripts:
- name: Clear caches
script: |
flutter clean
rm -rf $CM_BUILD_DIR/build
rm -rf $CM_BUILD_DIR/.gradle
